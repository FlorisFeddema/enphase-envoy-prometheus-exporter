apiVersion: v1
kind: Service
metadata:
  name: {{ include "enphase-envoy-prometheus-exporter.fullname" . }}
  labels:
    {{- include "enphase-envoy-prometheus-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "enphase-envoy-prometheus-exporter.selectorLabels" . | nindent 4 }}
