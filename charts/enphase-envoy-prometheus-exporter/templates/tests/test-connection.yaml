apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "enphase-envoy-prometheus-exporter.fullname" . }}-test-connection"
  labels:
    {{- include "enphase-envoy-prometheus-exporter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "enphase-envoy-prometheus-exporter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
